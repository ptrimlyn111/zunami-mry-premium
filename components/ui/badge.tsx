import { cn } from "@/lib/utils"; export function Badge({children,className}:{children:React.ReactNode;className?:string}){return <span className={cn("rounded-full border border-gold/30 bg-gold/10 px-3 py-1 text-xs font-medium text-gold",className)}>{children}</span>}
